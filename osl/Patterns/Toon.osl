shader TS_Toon(
    color Base = 1,
    color Edge = 0,
    float Angle = 50,
    output color Color = 0)
{
    float angle = cos(radians(Angle));

    if(abs(dot(-I,N)) > angle)
    {
        Color = Base;
    } else {
        Color = Edge;
    }
}
